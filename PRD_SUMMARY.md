# Project5 播客翻译工具 — PRD 摘要（仓库内置兜底版）

> 这是精简摘要，随本仓库 git 同步，保证即使当前环境访问不到外接盘也能拿到关键约束。
> 完整版（含各环节完成度表、原计划 vs 实际差距核对、决策历史索引）见工作区磁盘：
> `/Volumes/SANDISK ELE/AICoding/project5_podcast/PRD.md`（如果你的环境能访问该路径，请优先读完整版）。
> 最后从完整版同步：2026-07-20。

## 目标

把英文播客 / YouTube 演讲（AI Engineer、Figma Config、Cursor Compile 等大会分享居多）处理成中文配音音频，通过小宇宙 RSS 收听。**纯自用工具，不对外上架**。

## 每一集必须包含（目标成品）

- 完整中文配音 MP3（不是片段/小样）
- RSS 条目：标题（带序号）、简介（来源 + 原内容时间 + 嘉宾/主持人 + 亮点）、enclosure 音频直链
- 中文字幕 `.srt`
- shownotes（简介与亮点）

## 明确不做（非目标）

- 不做通用 SaaS / 多用户产品
- 不追求内容库覆盖率，只处理明确要看的视频/播客
- 不承诺实时/低延迟，按批处理节奏（小时到天级别）跑
- 不自动大规模抓取频道全部历史内容并翻译（成本控制）

## 当前最大阻塞：翻译层（截至 2026-07-20）

中文翻译没有稳定自动通道：

- Claude CLI 脚本化调用稳定返回 `403`，不可用
- OpenAI GPT 代码已就绪，但 `.env` 里没有真实 `OPENAI_API_KEY`
- YouTube 自动中文字幕已被明确否决，**不能**作为正式发布的翻译稿
- DeepSeek 计划接入，但代码里目前完全没有 DeepSeek 调用逻辑
- 第 4-8 集实际翻译是人工在对话里（Codex）完成的，**不是**脚本自动化

⚠️ **这一点是本项目此前"实现和最初 plan 差异较大"的核心原因之一**：只要翻译还是人工完成，就必须在当次工作的决策日志/PROGRESS 里如实记录"这次是人工做的，不是流水线自动完成"，不要让后续读者误以为翻译环节已经自动化。

## 发布前强制检查

- 不使用 YouTube 自动中文字幕作为正式翻译稿
- 每集文件名 / RSS 标题带序号（如 `09. xxx`）
- 简介写清楚来源、原内容时间、嘉宾/主持人、原始链接
- 多人对谈优先按说话人分声线（clone 或规则化时间段）

## 决策与进度记录（强制，两条都要做）

1. **决策**：写入 `/Volumes/SANDISK ELE/AICoding/project5_podcast/决策日志.md`（日期 + 结论 + why）。如果当前环境访问不到这个绝对路径，**必须明确告知用户"这次决策没能写进决策日志，因为路径不可达"**，不要静默跳过——这正是此前决策丢失的原因。
2. **进度**：更新本仓库根目录的 `PROGRESS.md`（当前状态 + 本次完成 + 验证 + 下一步 + 阻塞项）。
