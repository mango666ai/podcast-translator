# Project5 播客标准流程

> 最后更新：2026-07-21（Claude Code 整理，核对实际代码后重写）
> 这是唯一的标准流程定义，后续每一集都按这个走。跟旧版的区别见文末「本次修订说明」。

## 0. 现状速览

- 已发布 8 集，全部在 RSS 里，状态都是 `published`（第8集是 `published_multivoice`）。
- 机器可读的逐集状态表：[podcast_status.csv](podcast_status.csv)（字段含义见 §4）。
- 翻译层：**DeepSeek 已接入并验证通过**，是当前默认优先通道（2026-07-21 起）。

## 1. 入口

- 你在对话里直接发视频链接、播放列表或一组 URL，不用手动填表。
- 我负责整理标题、来源、优先级，**同时写入两处**：
  - 飞书多维表格「播客任务队列」——你能看的任务看板（`base_token=OucDbcF7MaNObBs1WANcmEAQnke`，`table_id=tblVsPdsuqoX5RLf`）
  - 本地 [podcast_status.csv](podcast_status.csv)——机器执行进度表
- 两处状态必须保持同步：每集状态变化（`transcribed`→`translated`→`dubbed`→`published`）时两边都要更新。
- ⚠️ **已知缺口**：`run_jobs.py`（飞书队列轮询器）内部调的是 `test_pipeline.py`（旧链路），不是现在实际在用的 `youtube_dub.py`（新链路），所以它当前**不能**端到端自动跑。要真正实现「填表格→自动出成品」，需要先把 `run_jobs.py` 改造到新链路上。在改造完成前，飞书表格作为**看板**同步维护，实际执行仍由对话驱动。
- **待办（未实现）**：你日常关注的 YouTube 频道自动发现新视频，方案是维护一份频道清单 + 定期抓频道 RSS，产出候选池供你确认，不自动开始翻译。见 [PROJECT_MAP.md §6](../PROJECT_MAP.md)。

## 2. 状态定义

| 状态 | 含义 | 是否算完成 |
| --- | --- | --- |
| `inbox` | 已收到链接，尚未整理入飞书表格/状态表 | 否 |
| `queued` | 已入队（飞书表格 + `podcast_status.csv` 都已登记），等待处理 | 否 |
| `downloaded` | 已下载原始英文音频 | 否 |
| `transcribed` | 已生成英文转写 | 否 |
| `translated` | 已生成中文翻译/字幕 | 否 |
| `sampled` | 已生成中文配音小样，等你试听 | 否 |
| `approved` | 你确认声音、语速、风格，可以全量生成 | 否 |
| `dubbed` | 已生成完整中文音频 | 是，待发布 |
| `published` | 已更新 RSS | 是 |
| `published_multivoice` | 已更新为按说话人区分声线的版本 | 是 |
| `failed` | 失败，必须在 `error` 列记录原因 | 否 |

## 3. 标准流程（逐步骤）

### 3.1 下载
- `yt-dlp` 下载音频，必要时用 `--cookies-from-browser chrome` 绕过登录校验。
- 原始音频只是素材，不是成品。

### 3.2 转写
- 默认用本地 `whisperX`（large-v3，CPU int8）。
- 如果本地转写太慢或被 YouTube 反爬拦住：改抓 **YouTube 自带的英文自动字幕**（`.vtt`/`.json3`，用 `vtt_to_segments.py` / `json3_to_segments.py` 清洗），当作转写文本的替代来源。
- **禁止**：YouTube 自带的**中文**自动字幕不能作为转写或翻译的输入来源，只能用于快速本地测试小样，正式发布前必须换掉。

### 3.3 翻译
- 统一走 `llm_openai.call_gpt()`——这是所有 LLM 调用（翻译、生成简介、起时间戳标签）的唯一入口函数，见 [llm_openai.py](llm_openai.py)。函数名带 `gpt` 是历史遗留，实际会按下面的优先级自动选通道；以后换模型只改这一个文件。
- 优先级：**DeepSeek > OpenAI > 本地 Claude CLI**，取第一个配了有效 key 的。
- Claude CLI 状态：2026-07-17 曾稳定返回 `403 Request not allowed`，**但 2026-07-21 重新实测已恢复正常**（`claude -p "只回复：ok" --model sonnet` 返回 ok；真实翻译型 prompt 带 JSON 输出约 8 秒返回，格式正确）。可作为可用兜底，不必再当成死路。
- 翻译提示词必须带上节目标题和来源信息（`source` 字段），风格自然口语化、意译为主，不逐字硬翻。
- 输出：双语分段 JSON（`start`/`end`/`text`/`text_zh`），是后续字幕、笔记、双语对照稿的唯一数据源。

### 3.4 字幕与文字稿（三个产物，互不替代）
| 产物 | 用途 | 生成函数 |
| --- | --- | --- |
| 中文 SRT 字幕 | 配合音频播放，带时间轴，供播放器/`podcast:transcript`同步显示 | `write_srt()` |
| 中英对照文字稿 `.md` | 逐句英文原文+中文翻译对照，带 `[mm:ss]` 时间戳，供核对翻译质量/阅读，**不**用于播放器同步 | `write_bilingual_transcript()` |
| 节目笔记 `.md` | 见 §3.5 | `write_notes_if_needed()` |

### 3.5 节目笔记（shownotes）— 固定六段式

```markdown
# <标题> - 节目笔记

## 来源
## 本期播客简介
## 本期嘉宾
## 时间戳
## 精彩内容
## 播客信息补充
```

- **来源**：大会/活动/播放列表/频道、原英文标题、讲者和公司；不确定的只写已确认部分，不臆造。**必须另起一行附上原始发布日期**（`原始发布日期：YYYY-MM-DD`，从 `podcast_status.csv` 的「原始发布日期」列取，代码里 `_publish_date_from_status()`；新收到链接时用 `yt-dlp --dump-json` 的 `upload_date` 字段填入该列，登记时就要填好），**再另起一行附上原始 YouTube 视频链接**（`原始视频：https://www.youtube.com/watch?v=<video_id>`，代码里 `_url_from_status()` 优先取 `podcast_status.csv` 的 `url` 字段，取不到则按 `video_id` 拼接）。原始发布日期要求 2026-08-06 起生效，视频链接要求 2026-07-21 起生效，均不回溯补全此前已发布的集数（除非单独决定要补）。
- **本期播客简介**：3-5 句，说清楚这期讲什么、为什么值得听。
- **本期嘉宾**：姓名、身份、所属公司；不确定的写"未在原视频中明确"，不编造。
- **时间戳**：**必须是真实数据锚定的时间点**，不是模型编的。做法：从双语分段里按时间间隔抽候选点，把候选点编号+文本片段交给 LLM，只让它选编号、起标签，时间本身由代码从候选点直接取（`build_timestamps()`），杜绝模型编时间。
- **精彩内容**：3-6 条，每条一句话，覆盖不同话题点。
- **播客信息补充**：必须包含"AI 中文译制版，声音克隆自原视频/主持人"的说明，并提示可能存在语气/断句不自然。

> 这是合并了旧版"一句话简介/节目简介/亮点/适合片头播报/可用标题"六段和你提供的参考格式"本期简介/本期嘉宾/时间戳/精彩内容/播客信息补充"五段后的定版，2026-07-21 起生效。已发布的前 3 集(`9fubhllmsBU`/`az6OEZV8iHw`/`ohKt066uFhg`)的 shownotes 还是旧格式，要不要批量重新生成成新格式，待你确认。

### 3.6 说话人与声线

**硬性要求：一律使用原声克隆，不用 MiniMax 预置音色**（`Wise_Woman`/`Calm_Woman` 这类只能用于临时 demo，不能进正式发布版）。

- **单说话人节目**：从原视频截取该讲者 1-2 分钟干净人声样本 → `clone_minimax_voice.py` 建 voice_id → 全片用这一个克隆声线。
- **多说话人节目**（主持人+主讲人+引用片段等）：每个 speaker 各自截样本、各建一个 voice_id，用 `youtube_multivoice_dub.py` 按 `<video_id>__speaker_rules.json`（`{start,end,speaker}` 列表）路由合成。

**说话人切分方式——目标是 AI 自动识别，当前尚未实现**：

| | 状态 |
| --- | --- |
| 目标 | 自动 diarization：模型听原音频，自动输出「谁在什么时间段说话」，生成 `speaker_rules.json` |
| 现状 | ❌ 未实现。第 8 集的规则是人工听后手写的时间段（只有 2 条规则） |
| 落地方案 | whisperX 自带 diarization，或 pyannote.audio |
| 前置条件 | 需要 HuggingFace token（pyannote 模型要授权同意条款）；CPU 上跑较慢；结果仍需抽查，AI 也会切错 |

在自动 diarization 接通前，多说话人节目仍靠人工标时间段兜底，但这是**临时状态**，不是目标流程。

- 当前只有第 8 集做了多声线，前 7 集是固定单一声线；是否回头给前 7 集补声线分离，属于单独的一次性工作量决策，不在标准流程默认步骤里。

### 3.7 声音克隆 + TTS
- 用 **MiniMax**（voice_clone + TTS），不是 F5-TTS（原计划里的开源方案已经被验证效果更差、更慢，正式弃用）。
- 逐段合成，网络抖动时自动重试（`_synthesize_with_retry`，最多3次，间隔递增），重试仍失败要显式报错中断，**不能**静默补静音进正式发布的音频。

### 3.8 中文配音小样 → 人工确认 → 完整音频
- 小样默认只做前 6 段，默认语速 `0.82`。
- 你确认声音、语速、翻译风格后，才生成全片完整音频，没确认不批量烧 MiniMax 额度。
- 完整音频文件名必须包含"完整中文音频"字样。

### 3.9 发布
- **发布节奏（2026-08-04 起）**：多集处理完的完整音频先留在本地 `youtube_dub/` 里，**不要一次性全部推上 RSS**，每天只发布一集，按 `dubbed` 状态排队。
- 复制完整 mp3 到发布仓库 `episodes/`，**改名成英文短 slug**（见 §5 命名规范的说明）。
- 复制中文 SRT 到 `transcripts/`。
- 更新 `feed.xml`：标题带序号、简介按 §3.5 六段结构、`enclosure` 指向 `episodes/` 下的直链、`podcast:transcript` 指向 SRT。
- `git add -A`（`youtube_demo/`、`work/`、`youtube_transcripts/`、`youtube_dub/` 等中间产物已在 `.gitignore`，不会被带上）→ commit → push。
- GitHub Pages 需要保持仓库 public 才能正常工作（免费版 Pages 不支持 private 仓库，之前踩过这个坑）。

### 3.10 各环节取舍现状

| 环节 | 状态 | 做/不做的区别 |
| --- | --- | --- |
| 开场女声旁白 | 暂缓 | 做：每集前面多约 60 秒中文开场白，更像正式节目。不做：直接从正文开始。代码 `intro_compose.py` 已有，只是没接进新链路 |
| ID3 章节标记 | ❌ 不做 | 做：mp3 内嵌章节点，支持的播放器能跳转段落。不做：整段音频无内部导航。**注意**：即使要做，也还缺「章节边界从哪来」的逻辑 |
| 飞书自动上传（`upload_feishu.py`） | ❌ 不做 | 做：成品 mp3+字幕自动存一份到飞书云盘，多一条独立于 GitHub/RSS 的备份和访问入口。不做：成品只在 GitHub 一条链路上。改动成本小，随时可启用 |
| 飞书任务队列自动化（`run_jobs.py`） | ⚠️ 表格同步维护，自动执行未接通 | 做：飞书表格填一行 → 脚本自动捞任务跑完整流程回写状态，可以丢一批链接不管。不做：靠对话驱动，我手动跑脚本，灵活但要盯着。**当前障碍**：`run_jobs.py` 调的是旧链路 `test_pipeline.py`，要接到 `youtube_dub.py` 才能真正自动跑 |
| 自动 diarization | ⚠️ 目标，未实现 | 见 §3.6：目标是 AI 听音频自动切说话人；当前靠人工标时间段兜底 |

## 4. `podcast_status.csv` 字段说明

| 字段 | 含义 |
| --- | --- |
| `video_id` | YouTube 视频ID，贯穿所有中间产物文件名 |
| `中文短标题` | 8-20字，人工/LLM拟定 |
| `原始发布日期` | 原视频发布日期（`YYYY-MM-DD`），来自 `yt-dlp --dump-json` 的 `upload_date`；新登记链接时就要填，节目笔记「来源」会自动带上 |
| `url` / `source` | 原始链接 / 来源说明（大会、频道、讲者） |
| `status` | 见 §2 状态表 |
| `downloaded`/`transcribed`/`translated`/`notes`/`tts_sample`/`full_tts`/`published` | 各环节完成标记（yes/no） |
| `error` | 出错原因，失败必须填 |
| `next_action` | 下一步要做什么 |

## 5. 命名规范

内部工作目录（`youtube_dub/<video_id>__<中文标题>/`）：

```text
<video_id>__<中文短标题>__<产物类型>.扩展名
```

示例：

```text
9fubhllmsBU__Fable5提示词最佳实践__双语分段.json
9fubhllmsBU__Fable5提示词最佳实践__中文字幕.srt
9fubhllmsBU__Fable5提示词最佳实践__双语对照.md
9fubhllmsBU__Fable5提示词最佳实践__简介与亮点.md
9fubhllmsBU__Fable5提示词最佳实践__完整中文音频_正常1.00.mp3
```

**同一集音频在两个地方有两个不同文件名**，发布时要改名：

| 位置 | 命名 | 例子 |
| --- | --- | --- |
| 内部工作目录 `youtube_dub/` | 中文，人看着方便 | `9fubhllmsBU__Fable5提示词最佳实践__完整中文音频_正常1.00.mp3` |
| 发布仓库 `episodes/` | 英文短 slug，全小写连字符 | `9fubhllmsBU-fable5-prompt-best-practices-zh.mp3` |

为什么要改名：公开目录的文件名会直接出现在 RSS 的音频 URL 里，中文会被转义成 `%E4%B8%AD%E6%96%87...` 一长串，不好读也容易在某些播客客户端出问题。

两者通过开头的 `video_id` 关联。**这层改名目前是人工做的**——每次发布要自己想一个英文 slug、手动复制改名，没有脚本自动生成。

## 6. 本次修订说明（2026-07-21）

对照实际代码核对后，这份文档相比旧版的变化：

1. 飞书多维表格**保留并同步维护**为任务看板（§1），但明确标注 `run_jobs.py` 的自动执行链路是断的（调旧链路 `test_pipeline.py`），要真自动化需要先改造。恢复了 `inbox` 状态。
2. 翻译层优先级更新为 DeepSeek（已验证）> OpenAI > Claude CLI。**更正**：旧描述说 Claude CLI 稳定返回 403 不可用，2026-07-21 重新实测已恢复正常（真实翻译型 prompt 约 8 秒返回，JSON 格式正确），改记为可用兜底。
3. 新增 §3.4/§3.5 的双语对照稿和六段式节目笔记——这两个功能 2026-07-21 刚实现并跑通冒烟测试；节目笔记"来源"部分必须附原始 YouTube 链接。
4. §3.6 明确硬性要求：**一律用原声克隆，不用 MiniMax 预置音色**；说话人切分的目标状态是 **AI 自动 diarization**，当前人工标时间段只是临时兜底（含落地方案和前置条件）。
5. §3.10 改为「各环节取舍现状」，逐项说明做与不做的实际区别，而不只是标记状态。
6. 删除了旧文档里"当前8个视频状态表"（已过时，8集全部发布，实时状态请查 [podcast_status.csv](podcast_status.csv)，不再在本文档里维护重复副本）。
7. 声音克隆技术路径明确记为 MiniMax，F5-TTS 正式标记弃用。
8. §5 补充说明为什么发布时要把中文文件名改成英文 slug。
