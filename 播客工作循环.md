# Project5 播客标准流程

> 最后更新：2026-07-21（Claude Code 整理，核对实际代码后重写）
> 这是唯一的标准流程定义，后续每一集都按这个走。跟旧版的区别见文末「本次修订说明」。

## 0. 现状速览

- 已发布 8 集，全部在 RSS 里，状态都是 `published`（第8集是 `published_multivoice`）。
- 机器可读的逐集状态表：[podcast_status.csv](podcast_status.csv)（字段含义见 §4）。
- 翻译层：**DeepSeek 已接入并验证通过**，是当前默认优先通道（2026-07-21 起）。

## 1. 入口

- 你在对话里直接发视频链接、播放列表或一组 URL，不用手动填表。
- 我负责整理标题、来源、优先级，写入本地 [podcast_status.csv](podcast_status.csv)。
- ~~飞书多维表格任务队列（`run_jobs.py`）~~：代码还在，但已确认没有用在任何一集的实际生产上，当前不作为标准流程的一部分。
- **待办（未实现）**：你日常关注的 YouTube 频道自动发现新视频，方案是维护一份频道清单 + 定期抓频道 RSS，产出候选池供你确认，不自动开始翻译。见 [PROJECT_MAP.md §6](../PROJECT_MAP.md)。

## 2. 状态定义

| 状态 | 含义 | 是否算完成 |
| --- | --- | --- |
| `queued` | 已记录，等待处理 | 否 |
| `downloaded` | 已下载原始英文音频 | 否 |
| `transcribed` | 已生成英文转写 | 否 |
| `translated` | 已生成中文翻译/字幕 | 否 |
| `sampled` | 已生成中文配音小样，等你试听 | 否 |
| `approved` | 你确认声音、语速、风格，可以全量生成 | 否 |
| `dubbed` | 已生成完整中文音频 | 是，待发布 |
| `published` | 已更新 RSS | 是 |
| `published_multivoice` | 已更新为按说话人区分声线的版本 | 是 |
| `failed` | 失败，必须在 `error` 列记录原因 | 否 |

## 3. 标准流程（逐步骤）

### 3.1 下载
- `yt-dlp` 下载音频，必要时用 `--cookies-from-browser chrome` 绕过登录校验。
- 原始音频只是素材，不是成品。

### 3.2 转写
- 默认用本地 `whisperX`（large-v3，CPU int8）。
- 如果本地转写太慢或被 YouTube 反爬拦住：改抓 **YouTube 自带的英文自动字幕**（`.vtt`/`.json3`，用 `vtt_to_segments.py` / `json3_to_segments.py` 清洗），当作转写文本的替代来源。
- **禁止**：YouTube 自带的**中文**自动字幕不能作为转写或翻译的输入来源，只能用于快速本地测试小样，正式发布前必须换掉。

### 3.3 翻译
- 统一走 `llm_openai.call_gpt()`，优先级：**DeepSeek > OpenAI > 本地 Claude CLI**（Claude CLI 脚本调用已知会稳定返回 403，只作最后兜底，不要依赖它）。
- 翻译提示词必须带上节目标题和来源信息（`source` 字段），风格自然口语化、意译为主，不逐字硬翻。
- 输出：双语分段 JSON（`start`/`end`/`text`/`text_zh`），是后续字幕、笔记、双语对照稿的唯一数据源。

### 3.4 字幕与文字稿（三个产物，互不替代）
| 产物 | 用途 | 生成函数 |
| --- | --- | --- |
| 中文 SRT 字幕 | 配合音频播放，带时间轴，供播放器/`podcast:transcript`同步显示 | `write_srt()` |
| 中英对照文字稿 `.md` | 逐句英文原文+中文翻译对照，带 `[mm:ss]` 时间戳，供核对翻译质量/阅读，**不**用于播放器同步 | `write_bilingual_transcript()` |
| 节目笔记 `.md` | 见 §3.5 | `write_notes_if_needed()` |

### 3.5 节目笔记（shownotes）— 固定六段式

```markdown
# <标题> - 节目笔记

## 来源
## 本期播客简介
## 本期嘉宾
## 时间戳
## 精彩内容
## 播客信息补充
```

- **来源**：大会/活动/播放列表/频道、原英文标题、讲者和公司；不确定的只写已确认部分，不臆造。**必须另起一行附上原始 YouTube 视频链接**（`原始视频：https://www.youtube.com/watch?v=<video_id>`，代码里 `_url_from_status()` 优先取 `podcast_status.csv` 的 `url` 字段，取不到则按 `video_id` 拼接）。2026-07-21 起生效，此前已发布的集数不回溯补链接。
- **本期播客简介**：3-5 句，说清楚这期讲什么、为什么值得听。
- **本期嘉宾**：姓名、身份、所属公司；不确定的写"未在原视频中明确"，不编造。
- **时间戳**：**必须是真实数据锚定的时间点**，不是模型编的。做法：从双语分段里按时间间隔抽候选点，把候选点编号+文本片段交给 LLM，只让它选编号、起标签，时间本身由代码从候选点直接取（`build_timestamps()`），杜绝模型编时间。
- **精彩内容**：3-6 条，每条一句话，覆盖不同话题点。
- **播客信息补充**：必须包含"AI 中文译制版，声音克隆自原视频/主持人"的说明，并提示可能存在语气/断句不自然。

> 这是合并了旧版"一句话简介/节目简介/亮点/适合片头播报/可用标题"六段和你提供的参考格式"本期简介/本期嘉宾/时间戳/精彩内容/播客信息补充"五段后的定版，2026-07-21 起生效。已发布的前 3 集(`9fubhllmsBU`/`az6OEZV8iHw`/`ohKt066uFhg`)的 shownotes 还是旧格式，要不要批量重新生成成新格式，待你确认。

### 3.6 说话人与声线
- 当前**没有自动 diarization**，人工划时间段规则（`<video_id>__speaker_rules.json`，`{start,end,speaker}` 列表）。
- 单说话人节目：全片用一个 MiniMax 克隆 voice_id。
- 多说话人节目（主持人+主讲人+引用片段等）：先人工听一遍标出时间段，每个 speaker 单独截取参考音频、`clone_minimax_voice.py` 建一个 voice_id，再用 `youtube_multivoice_dub.py` 按规则路由合成。
- 当前只有第 8 集做了多声线，前 7 集是固定单一声线；是否回头给前 7 集补声线分离，属于单独的一次性工作量决策，不在标准流程默认步骤里。

### 3.7 声音克隆 + TTS
- 用 **MiniMax**（voice_clone + TTS），不是 F5-TTS（原计划里的开源方案已经被验证效果更差、更慢，正式弃用）。
- 逐段合成，网络抖动时自动重试（`_synthesize_with_retry`，最多3次，间隔递增），重试仍失败要显式报错中断，**不能**静默补静音进正式发布的音频。

### 3.8 中文配音小样 → 人工确认 → 完整音频
- 小样默认只做前 6 段，默认语速 `0.82`。
- 你确认声音、语速、翻译风格后，才生成全片完整音频，没确认不批量烧 MiniMax 额度。
- 完整音频文件名必须包含"完整中文音频"字样。

### 3.9 发布
- 复制完整 mp3 到发布仓库 `episodes/`（重命名为公开用的英文短 slug，如 `9fubhllmsBU-fable5-prompt-best-practices-zh.mp3`）。
- 复制中文 SRT 到 `transcripts/`。
- 更新 `feed.xml`：标题带序号、简介按 §3.5 六段结构、`enclosure` 指向 `episodes/` 下的直链、`podcast:transcript` 指向 SRT。
- `git add -A`（`youtube_demo/`、`work/`、`youtube_transcripts/`、`youtube_dub/` 等中间产物已在 `.gitignore`，不会被带上）→ commit → push。
- GitHub Pages 需要保持仓库 public 才能正常工作（免费版 Pages 不支持 private 仓库，之前踩过这个坑）。

### 3.10 当前明确不做的环节
| 环节 | 状态 | 备注 |
| --- | --- | --- |
| 开场女声旁白 | 暂缓 | 认可价值，以后再接回 `intro_compose.py`，当前流程不包含这一步 |
| ID3 章节标记 | 不做 | 已从计划划掉 |
| 飞书自动上传 | 不做 | 已从计划划掉 |
| 飞书任务队列自动化 | 不用 | 代码在但没接入新链路，继续对话驱动 |
| 自动 diarization | 未实现 | 见 §3.6，人工规则代替 |

## 4. `podcast_status.csv` 字段说明

| 字段 | 含义 |
| --- | --- |
| `video_id` | YouTube 视频ID，贯穿所有中间产物文件名 |
| `中文短标题` | 8-20字，人工/LLM拟定 |
| `url` / `source` | 原始链接 / 来源说明（大会、频道、讲者） |
| `status` | 见 §2 状态表 |
| `downloaded`/`transcribed`/`translated`/`notes`/`tts_sample`/`full_tts`/`published` | 各环节完成标记（yes/no） |
| `error` | 出错原因，失败必须填 |
| `next_action` | 下一步要做什么 |

## 5. 命名规范

内部工作目录（`youtube_dub/<video_id>__<中文标题>/`）：

```text
<video_id>__<中文短标题>__<产物类型>.扩展名
```

示例：

```text
9fubhllmsBU__Fable5提示词最佳实践__双语分段.json
9fubhllmsBU__Fable5提示词最佳实践__中文字幕.srt
9fubhllmsBU__Fable5提示词最佳实践__双语对照.md
9fubhllmsBU__Fable5提示词最佳实践__简介与亮点.md
9fubhllmsBU__Fable5提示词最佳实践__完整中文音频_正常1.00.mp3
```

发布仓库 `episodes/`（对外公开目录）用英文短 slug，不用中文：

```text
9fubhllmsBU-fable5-prompt-best-practices-zh.mp3
```

两者通过 `video_id` 关联，目前这层改名是人工做的，没有脚本自动化。

## 6. 本次修订说明（2026-07-21）

对照实际代码核对后，这份文档相比旧版的变化：

1. 删除了"飞书队列驱动入口"的描述——核实后发现从未真正用于生产这 8 集，旧文档一直在描述一个没被使用的流程。
2. 翻译层优先级更新为 DeepSeek（已验证）> OpenAI > Claude CLI，删除了旧文档里"claude headless 免费走 Max 订阅"的描述（已确认该路径脚本调用返回403，不可用）。
3. 新增 §3.4/§3.5 的双语对照稿和六段式节目笔记——这两个功能今天（2026-07-21）刚实现并跑通冒烟测试。
4. 明确列出 §3.10"当前不做"清单，避免继续误以为开场白/ID3章节/飞书上传是"已完成"（这几项在旧版 `PROGRESS.md` 的里程碑表里曾标记为✅，但从未接入这条 `youtube_dub.py` 新链路）。
5. 删除了旧文档里"当前8个视频状态表"（已过时，8集全部发布，实时状态请查 [podcast_status.csv](podcast_status.csv)，不再在本文档里维护重复副本）。
6. 声音克隆技术路径明确记为 MiniMax，F5-TTS 正式标记弃用。
