# Project5 播客工作循环

> 目标：把你在聊天里发来的英文视频 / 播客链接，稳定处理成可听的中文音频。英文转写、英文原始音频、小样都不是最终完成；只有完整中文音频通过验收，才算完成。

## 1. 入口

- 你不用手动填飞书表单。
- 你在对话里直接发视频链接、播放列表或一组 URL。
- 我负责整理标题、来源、优先级、备注，并写入飞书多维表格「播客任务队列」。
- 飞书多维表格作为你能看的任务看板；本地 `podcast_status.csv` 作为机器执行进度表。

## 2. 状态定义

| 状态 | 含义 | 是否算完成 |
| --- | --- | --- |
| `inbox` | 已收到链接，待入飞书或待整理 | 否 |
| `queued` | 已入飞书，等待处理 | 否 |
| `downloaded` | 已下载原始英文音频 | 否 |
| `transcribed` | 已生成英文转写 `.txt/.json` | 否 |
| `translated` | 已生成中文翻译 / 中文字幕 | 否 |
| `sampled` | 已生成中文配音小样，等你试听 | 否 |
| `approved` | 你确认声音、语速、风格可以全量生成 | 否 |
| `dubbed` | 已生成完整中文音频 | 是，待发布 |
| `published` | 已更新 RSS / 小宇宙或上传飞书成品 | 是 |
| `failed` | 失败，必须记录错误原因 | 否 |

## 3. 标准流程

1. `聊天链接 -> 飞书队列`
   - 批量解析链接。
   - 重复 URL 不重复创建，优先更新备注和优先级。
   - 状态写为 `queued` / `待处理`。

2. `下载 -> 英文转写`
   - 输出到 `youtube_transcripts/`。
   - 原始英文音频必须放在 `_audio/`，只能视为素材，不能当成成品。

3. `中文翻译`
   - 输出双语分段 JSON 和中文字幕 `.srt`。
   - 翻译风格：自然口语化，适合朗读，不逐字硬翻。

4. `简介与亮点`
   - 每集必须生成 `__简介与亮点.md`。
   - 内容包括：一句话简介、节目简介、3-5 个亮点、片头播报文案、可用标题。
   - 这个文件用于小宇宙 shownotes、飞书看板和后续片头音频。

5. `中文配音小样`
   - 默认只做前 6 段，避免浪费 TTS 额度。
   - 默认语速 `0.82`，因为上一版小样偏快。
   - 小样必须生成中文可读文件名。

6. `人工确认`
   - 你确认声音、语速和翻译风格后，才生成完整中文音频。
   - 没确认前，不批量烧 MiniMax TTS 额度。

7. `完整中文音频`
   - 输出完整 `.mp3`，再生成字幕、上传飞书或更新 RSS。
   - 完整中文音频才可标记为 `dubbed`。

## 4. 命名规范

命名格式：

```text
<video_id>__<中文短标题>__<产物类型>.扩展名
```

示例：

```text
9fubhllmsBU__Fable5提示词最佳实践__中文配音小样_06段_慢速0.82.mp3
9fubhllmsBU__Fable5提示词最佳实践__中文字幕.srt
9fubhllmsBU__Fable5提示词最佳实践__双语分段.json
```

规则：

- 保留 `video_id`，方便追溯 YouTube 来源。
- 中文短标题控制在 8 到 20 个字左右。
- 原始英文音频目录命名为 `_audio/`，不叫成品。
- 完整中文音频必须包含 `完整中文音频` 字样。
- 小样必须包含 `小样`、段数和语速。

## 5. 当前 8 个视频

| video_id | 中文短标题 | 当前状态 | 备注 |
| --- | --- | --- | --- |
| `9fubhllmsBU` | Fable5提示词最佳实践 | `sampled` | 已生成新的慢速中文小样，等待试听确认 |
| `ohKt066uFhg` | Slack里的AI员工 | `transcribed` | 已有英文转写 |
| `az6OEZV8iHw` | Cursor界面改版设计 | `transcribed` | 已有英文转写 |
| `l5VRhrNeidY` | AI时代面向人类写作 | `transcribed` | 已有英文转写 |
| `4CAFK-rc26A` | AI时代PM职能演化 | `transcribed` | 已有英文转写 |
| `otkSbl399tU` | 2000年代设计灵感 | `transcribed` | 已有英文转写 |
| `u7_Qof3AJ1g` | Notion设计系统落地 | `downloaded` | 已下载英文音频，未看到转写完成产物 |
| `Yz03ERsfDDM` | Brat封面设计师经历 | `queued` | 未看到下载 / 转写产物 |

## 6. 下一步

1. 你先试听 `9fubhllmsBU` 的慢速中文小样。
2. 若语速、声音、翻译风格满意，先生成 `9fubhllmsBU` 的完整中文音频。
3. 同时补齐 `u7_Qof3AJ1g` 和 `Yz03ERsfDDM` 的英文转写。
4. 第一个完整音频通过后，再批量推进剩下 7 个。
5. 后续每个视频都必须同时产出 `__简介与亮点.md`。
