# 技术债清单

> 已知的坏味道/短板，都是**当时绕过去了、没修**的。放这里是为了不被 PROGRESS.md 的时间线冲走。
> 修完一条就删掉它，并在 PROGRESS.md 里记一笔。
> 跟 [决策日志.md](../决策日志.md)（记"为什么这样定"）、[PROGRESS.md](PROGRESS.md)（记"做到哪里"）不重复。

---

## 1. 发布时的中英文改名靠人工

**现状**：内部工作文件名用中文（`<video_id>__<中文标题>__完整中文音频_*.mp3`），发布到 `episodes/` 要改成英文短 slug（`<video_id>-english-slug-zh.mp3`），因为公开 URL 里的中文会被转义成一长串 `%E4%B8%AD...`。

**债在哪**：这层改名每次都得人工想一个英文 slug、手动复制改名，没有脚本。容易手滑打错、风格不统一。

**可能的修法**：让 LLM 从标题生成 slug（要保证幂等/可复现），或者在 `podcast_status.csv` 加一列 `slug` 提前定好。

**优先级**：低。纯手工麻烦，不会导致故障。

---

## 2. `run_jobs.py`（飞书任务队列）还指向旧链路，自动执行跑不通

**现状**：[run_jobs.py:189](run_jobs.py) 调的是 `test_pipeline.py`，以及 `tts_minimax.py` / `intro_compose.py` / `add_chapters.py` / `upload_feishu.py` 这一整套**旧链路**。而实际在用的是 `youtube_transcribe.py` → `youtube_dub.py` → `youtube_multivoice_dub.py` 的新链路。

**后果**：「飞书表格填一行 → 自动出成品」这条路是断的。飞书多维表格当前只作为**看板**同步维护，实际执行仍靠对话驱动。

**另外**：旧链路里的 `intro_compose.py`（开场旁白）、`add_chapters.py`（ID3章节）、`upload_feishu.py`（飞书上传）三个环节已经明确不在标准流程里了（见 [播客工作循环.md](播客工作循环.md) §3.10），所以照搬旧链路的调用顺序也不对，需要按新流程重写编排。

**优先级**：低-中。取决于是否真的需要"丢一批链接不管"的自动化；目前对话驱动够用。

---

## 3. 自动 diarization 没接通（不算债，算未实现的目标）

目标是 AI 听音频自动切说话人。`pyannote.audio` 路线卡在 HF gated 模型授权（用户遇到 hf.co 418 报错，未解决）。当前用的是**文本层面**的替代方案：靠字幕里的 `>>` 说话人切换标记 + DeepSeek 判断内容归属，绕开了音频模型和 HF token 依赖。

替代方案实际够用（`youtube_multivoice_dub.py` 只需要「时间段→speaker 标签」，从不读原始音频做声纹分析），所以这条**不急**。但它依赖字幕里有 `>>` 标记，遇到没有说话人标记的字幕源会失效。

详见 [播客工作循环.md](播客工作循环.md) §3.6。
